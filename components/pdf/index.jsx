import jsPDF from "jspdf";
import Template from "./template";

const PDF = () => {
    const doc = new jsPDF({
        unit: "px",
        format: "a4",
    });

    return <div></div>;
};

export default PDF;
